{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Playwright MCP (SSE)",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/cli.js",
            "args": [
                "--port",
                "8080" // 或者您希望使用的端口
                // 添加其他需要的命令行参数
            ]
        }
    ]
}